scraper/.geocache.json
**/__pycache__/
*.pyc
.DS_Store
Thumbs.db

# Note: scraper/.sniper_state.json IS tracked on purpose — the hourly cloud
# routine clones the repo fresh each run, so the diff state has to persist in git.
